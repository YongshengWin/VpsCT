# 变更记录

[返回 README](README.md)

## 1. v0.1.2 — 2026-09-16

### 1.1 面板与数据安全

- 修正服务器安全状态提示：使用“更新文件校验与本机安全策略”，不再把免签名密钥模式描述为独立验签；同步修正旧版与策略加载失败提示。

- 完善管理接口角色检查、Origin/CSRF、Host 与可信反向代理边界，使用安全会话 Cookie；账号安全状态变化后撤销旧会话，SSE 持续检查权限。
- 外部订阅请求限制协议、地址、重定向、响应大小和并发，实际连接绑定已校验 IP；默认拒绝访问本机、私网和元数据地址，减少 SSRF 与 DNS 重绑定风险。
- 加强头像解码重编码、CSP、订阅参数白名单与输出转义，限制解析、密码校验、请求和日志的资源开销；注册令牌单次消费、设备配置绑定和日志去重。
- 敏感字段加密保存，新增独立密钥和加密一致性备份；恢复时清理旧会话与注册能力。数据库迁移至 schema 9，备份与恢复必须同时保存相应密钥及状态。

### 1.2 更新与维护安全

- 默认使用官方 HTTPS 下载与 SHA256 完整性校验，不需要发布签名密钥、独立元数据服务或预置验证器。agent 对照固定官方发行源校验，内核限制固定上游下载地址。
- 网络处理使用专用低权限身份，root 端仅执行固定且受本机策略允许的操作；网页不能任意指定命令、安装来源、路径或私网权限。
- 升级前核对恢复材料完整性与安全底线；补充磁盘/inode 预算、日志轮转及清理限制，保留必要恢复数据。
- v0.1.1 可直接使用新版安装器升级；停服前验证下载与恢复文件，失败自动回滚。默认校验依赖 GitHub/上游 HTTPS，不提供独立发布签名、签名撤销或安全代次保证。可选签名配置说明见 [安全迁移](docs/security-migration.md)。

### 1.3 代理隔离与运行期保护

- sing-box 和 Snell 使用专用低权限账户，移除代理 NET_ADMIN，限制原始套接字；代理不能读取管理数据库、维护 socket 或源证书目录。
- 普通节点继续共享 sing-box 进程；仅同时配置私网授权节点时分为两个权限组。Snell 仍每节点一个官方进程，流量公式与节点身份不变。
- cgroup 出站规则独立于计量标记，防止通过修改 mark 绕过受保护目的地址限制；新增有效 mark 白名单，拦截未知、跨权限组和失效标记。
- 新增每 5 秒运行的本地防火墙守护：规则丢失或变更时原子修复；恢复失败则撤销启动许可并强制停止代理，恢复后仅启动守护器暂停且仍有效的服务。检查不依赖控制端或 agent 轮询，不重置计费计数器。
- 运行配置移至 `/etc/ctlvps-proxy`，保留管理目录权限；证书采用验证后的快照，原生 ACME 保留账户与状态，配置切换失败恢复原服务。

### 1.4 验证与限制

- 已通过隔离环境的 96 项网络边界、六种 sing-box 协议的 24 条 IPv4/IPv6 TCP/UDP 路径、正常计量、故障恢复、17 项卸载及目标机原生 systemd 验证。
- 官方 Snell 5.0.1 与 OpenSnell 测试客户端的密码认证、IPv4/IPv6 TCP/UDP、连接复用和错误密码拒绝通过；未覆盖 Surge 专用 QUIC 模式。
- 测试 CA 的完整 ACME 签发、自动续期、无重启热加载、账户保留及重启持久化通过；公有 CA 的实际域名和公网挑战路径需在启用时验收。
- 守护器在本地 Linux ARM64、小规模规则下连续 15 次检查的 cgroup 内存峰值约 3.3–3.8 MiB，耗时中位数约 11 ms；不是全部机器、冷启动或大规模规则的资源上限。
- 保留共享进程意味着代理被完全控制后仍可能冒用同组有效节点标记；原生 ACME 保留零 mark 例外。规则守护存在检测间隔，不抵抗宿主 root 同时禁用保护。
- 首次代理权限切换可能短暂断连；安装、迁移和生产部署须按 [验证记录](docs/security-proxy-hardening-implementation.md) 与本机策略执行。不能仅替换旧二进制回退数据库迁移。

## 2. v0.1.1 — 2026-09-16

### 2.1 进程与内存优化

- 同一 VPS 的 sing-box 节点合并为一个进程，保留各自端口、认证信息及流量归属，减少重复运行时开销。配置先校验再切换，失败恢复原服务。
- 连接日志缓冲增加条数和字节预算，错误日志仅读取有限尾部，降低日志堆积造成的内存增长。日志丢弃不参与流量计费。
- 代理服务纳入整体内存预算。一次 Linux amd64 灰度中，6 个 sing-box 进程合并后 PSS 从约 84 MiB 降至约 39 MiB；该观测不是高并发容量承诺。

### 2.2 流量与面板

- sing-box 按节点标记连接，使用 nftables 分别统计客户端与目标站点连接的入站、出站字节；Snell 使用独立、跨服务重启保留的 systemd 计数 slice，不与 nft 重复相加。
- 迁移前停止旧实例并结算最终计数；待确认结算持久保存，可幂等重试。计数、历史用量和分享用量在同一事务更新，避免重试重复计费；保留已删除节点的计量身份。
- 服务器流量卡片可直接选择整台服务器或具体节点，查看近 7 / 30 / 90 天入站、出站、汇总及趋势。节点列表新增近 30 天用量、服务器筛选及用量排序。
- 未采集、零用量和加载失败分别显示，计量异常可见。服务器仍按网卡计量，包含 SSH、系统更新等流量，不应与节点总和强制相等。

### 2.3 升级与兼容

- 数据库迁移至 schema 7。先升级控制端，再升级 agent；先备份数据库、agent 状态和配置。恢复旧版需匹配的数据库与状态备份，不能仅替换旧二进制。
- 控制端发行包包含新版 agent，升级后可能自动同步所有已接入 VPS，并在节点合并时造成短暂重连。建议先灰度，再逐步推广。
- 本机灰度可用 agent 的 `--hold-updates` 固定版本，同时暂停自动同步和网页维护；完成正式分发后移除此参数并重启，恢复同步。
- 计量需要 nftables、conntrack 和支持 IPAccounting 的 systemd；保留路由标记空间冲突时拒绝应用。

### 2.4 Snell 内存风险与其他限制

- **Snell 仍为每个独立节点启动一个官方服务端进程，没有实现多用户单进程、按需启动或空闲回收。独立节点和并发增加时，进程数与内存仍可能持续增长。**
- **整体内存上限只是保护措施，不会消除 Snell 的重复开销；达到上限可能触发 OOM 杀进程并中断连接。不要将大量 Snell 节点直接部署到低内存 VPS。** 共用密码虽然可共用实例，但不提供独立用户认证、撤销和计费，本版没有采用此方案。
- 单进程 sing-box 的内存仍随并发连接、协议缓冲增长，并非用户数增加后内存恒定。
- 突然断电或手工清空内核计数器可能丢失最后一次上报后的字节；旧版本未采集的历史不能补算。跨账期采样增量归到采样结束时间，不能恢复秒级跨月分摊。
- 已验证隔离环境 Shadowsocks 的 IPv4/IPv6、TCP/UDP 逐字节计量，真实 systemd 生命周期、Snell 计数 slice 保留，以及一台线上 6 节点灰度的计量衔接。该机 AnyTLS / VLESS Reality 通过回环客户端认证和 HTTPS 转发；两份分享的观测增量与所属节点增量一致。未声称完成 100 用户并发压测或所有协议、系统的实机验收。

## 3. v0.1.0 附件更新 — 2026-09-15

本次沿用 v0.1.0，更新原有发行附件并同步源码标签。早期安装需通过终端重新执行 `--update`，以安装维护服务；已有数据与账户保留。

### 3.1 安装与卸载

- 新增独立卸载器，支持分别卸载控制端、agent 或本机两端，默认保留配置与数据；显式支持清空数据、预览、非交互确认和独占 Caddy 站点清理。
- agent 卸载同步停止其部署的服务；按角色清理共享目录，拒绝自定义启动配置、危险软链接父目录及嵌套挂载点。
- 发行流程新增卸载附件与 SHA256 校验，控制端安装后提供本地卸载入口。脚本兼容 v0.1.0 默认 systemd 安装，发行附件同步更新。
- 补充 Cloudflare HTTPS、同机部署地址区分及服务连接排查说明。
- README 安装和更新命令统一使用最新正式版入口，无需手动修改版本号；指定版本作为可选用法。

### 3.2 网页维护

- 设置页支持检查控制端正式版本、升级和卸载；服务器详情支持 agent 升级、卸载及任务进度。
- 服务器详情的维护操作收进「更多」，已完成任务记录在弹窗内查看；仅执行中或异常任务显示简短提示。按文件校验值判断 agent 是否已同步，兼容同版本附件更新。
- 「重新下发配置」改为「重新应用节点配置」，与 agent 维护集中在同一菜单，维护执行中暂时禁用。
- 固定操作的本地维护服务与独立 systemd worker，使网页、控制端或 agent 退出后任务仍可执行。管理员密码、两步验证、目标名称确认和按角色数据清理限制操作范围。
- 控制端升级支持备份与失败回退；agent 升级校验下载文件与运行程序，失败恢复旧程序。维护任务持久保存，拒绝重复执行及过期领取。
- 安装器改在 `/opt` 下暂存待执行程序，支持 `/tmp` 为 `noexec` 的系统。

## 4. v0.1.0 首次发布 — 2026-09-15

### 4.1 服务器管理

- 将服务器详情中的操作明确为「检查 agent 更新」和「重新下发配置」，更新提示说明自动同步状态；缺少对应分发文件时不再提示版本已最新。

### 4.2 模板与预设易用性

- AI 指令按 Clash / mihomo、Surge、Shadowrocket、sing-box 分开，支持改造已有配置和从零编写；选择后直接复制指令，在模板编辑器中提供字段、占位符与渲染限制说明。
- 指令预览随选项切换，并与复制内容一致。
- 将「代理组预设」明确为「分组与规则预设」，解释应用位置、复制关系和客户端差异。订阅统一成套应用分组与规则，并可恢复模板默认设置。

### 4.3 功能精简

- 移除订阅页的 Clash 配置上传与原样托管，统一使用节点和独立模板生成配置。旧上传订阅保留记录及数据库中的原文，但停止下载、编辑和重新启用；可由管理员删除。

### 4.4 开源发布准备

- 控制端一键安装器：Debian / Ubuntu、Linux amd64 / arm64，版本文件校验、systemd、可选 Caddy HTTPS。
- 安装器升级前停服备份；保留配置和账户，拒绝覆盖现有手动安装。
- 首次管理员初始化需要本地令牌，并原子地限制为一个初始管理员。
- 在线 IP 归属地查询改为主动开启，默认使用本地数据库。
- 干净环境构建、双架构发行包、CI、安全扫描、依赖许可证和社区文档。
- README 与说明文档使用统一层级编号，按安装、使用、维护和发布顺序组织；发行包补齐相互链接的根目录文档。
- Docker 新安装默认使用命名卷，容器以非 root 用户运行。既有 bind mount 部署请保留原挂载或按运维文档迁移。
- 构建基线更新为 Go 1.26.8，前端更新 Vite 与 React Router 以修复已知漏洞。

### 4.5 首版功能

- 服务器与 agent 管理；sing-box / Snell 节点部署和期望状态收敛。
- 节点库、外部订阅导入、代理组与规则、多客户端订阅格式。
- 独立分享、流量统计、配额、到期与撤销。
- TOTP、审计、连接日志、Telegram 通知和数据库备份。

### 4.6 兼容性与验证范围

- 首个公开版本；已有开发版升级前应完整备份数据目录。控制端启动时执行内置数据库迁移，恢复旧程序时应同时恢复对应备份。
- 发行包包含对应架构控制端及双架构 agent。支持自动更新的 agent 会随心跳同步控制端分发的版本；配置中的服务组件版本仍由管理员选择并保存。
- 安装器面向 Debian / Ubuntu、systemd、Linux amd64 / arm64。安装器、容器及既有实机部署的验证范围见 [验收记录](docs/readiness.md)，交叉编译不等同于各架构全新主机验收。
- 发行附件提供 SHA256 校验，尚未提供独立发布签名。
