# 安全问题反馈

[返回 README](README.md)

## 1. 私密报告

请通过 [Security → Report a vulnerability](https://github.com/YongshengWin/VpsCT/security/advisories/new) 提交私密漏洞报告。

如果页面没有该入口，请先提交一个只询问私密联系渠道的普通 Issue，不要附上漏洞细节、复现载荷或任何凭据。

报告可包含：受影响的 VpsCT / agent 版本、部署方式、影响范围、使用虚构数据的复现步骤、建议修复方式。请勿提交第三方系统的数据，也不要在未授权系统上测试。

## 2. 支持范围

项目处于早期阶段，安全修复优先提供给最新正式版本；尚未承诺旧版本长期维护或固定响应时限。遇到已确认问题时会说明受影响版本和缓解方式。

## 3. 部署边界

- 管理员能够访问和改变服务凭据、配置访问链接、外部配置来源、资源分享和服务器配置。仅向可信人员分配管理员权限。
- 公网控制端应通过 HTTPS 访问，后端默认由安装器绑定 `127.0.0.1:8080`。仅在后端访问受限且 HTTPS 入口可信时启用 `CTLVPS_TRUST_PROXY`。
- 首次管理员创建需要本地 `setup-token`，令牌文件权限为 `0600`；初始化成功后失效并删除。现有安装的管理员不会被重置。
- SQLite、备份和 agent 状态中包含敏感配置，应限制读取权限。备份文件不因添加了 `.gitignore` 而自动加密。
- `/s/` 与 `/r/` 链接是访问凭据。不要放入公开 Issue、截图或未经脱敏的连接日志。
- agent 以 root 执行系统部署操作，只连接配置的控制端；保护控制端与安装来源同样重要。
- SHA256 清单检查文件完整性；如果发布者账号或整个发布渠道被攻破，单独的同渠道校验清单不能验证发布者身份。

数据采集与第三方请求见 [隐私说明](docs/privacy.md)。
